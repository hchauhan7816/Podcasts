import 'package:flutter/material.dart';

// Colors that are used in this application
const secondaryColor = Color(0xFFFFCA28);
const backgroundColor = Color(0xFFF1EFF1);
const primaryColor = Color(0xFF035AA6);
const textLightColor = Color(0xFF747474);
const textColor = Color(0xFF000839);
const blueColor = Color(0xFF40BAD5);

const defaultMargin = 25.0;
const defaultPadding = 20.0;
const cardMargin = 13.0;
const cardPadding = 20.0;

const shadowProp = BoxShadow(
  offset: Offset(0, 15),
  blurRadius: 25,
  color: Colors.black12,
);
